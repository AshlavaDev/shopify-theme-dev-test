{% case media.media_type %}
  {% when 'image' %}
    <img src="{{ media | img_url: 'large' }}" alt="{{ media.preview_image.alt }}" class="w-full h-full object-cover">
  {% when 'external_video' %}
    {{ media | external_video_tag: autoplay: false, controls: true, loop: true, muted: true, playsinline: true, class: 'w-full h-80 object-cover' }}
  {% when 'video' %}
    {{ media | video_tag: autoplay: false, controls: true, loop: true, muted: true, playsinline: true, class: 'w-full h-80 object-cover' }}
  {% when 'model' %}
    {{ media | model_viewer_tag: class: 'w-full h-80 object-cover' }}
  {% else %}
    {{ media | media_tag }}
{% endcase %}