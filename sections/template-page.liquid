<div class="flex flex-col items-center justify-center py-4 px-4 mx-auto gap-4 max-w-3xl">
  <h1 class="text-xl md:text-3xl">{{ page.title }}</h1>
  <section>
    {{ page.content }}
  </section>
</div>